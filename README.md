# Simple-Spec-Flow(SSF)

SSF — это последовательный процесс подготовки фичи к разработке: от формулирования бизнес-контекста до конкретного плана действий, списка задач и проверки результатов. Он помогает синхронизировать ожидания продуктовой, инженерной и QA-команд, уменьшить количество изменений «на лету» и зафиксировать критерии успешности до написания кода. В рамках ssf все артефакты генерирует LLM: пользователь формулирует контекст и параметры, а ассистент создаёт и сохраняет нужные файлы.

## Зачем нужен ssf

- **Единая картина.** Все артефакты (спецификация, план, задачи, отчёты проверки, сводки хотфиксов) живут в одном месте и связаны между собой.
- **Прозрачность ожиданий.** Команда заранее понимает, что именно нужно построить, какие ограничения и риски учесть.
- **Готовность к выполнению.** По итогам ssf появляется понятный backlog, который можно раскладывать по исполнителям.

## Структура каталога `/spec`

```
/spec
├── README.md            ← это руководство
└── core                 ← шаблоны, запускающие ssf
    ├── spec.md          ← шаблон спецификации (что делаем и зачем)
    ├── plan.md          ← шаблон плана реализации (как делаем)
    ├── tasks.md         ← шаблон списка задач (кто и что проверяет)
    ├── verify.md        ← шаблон отчёта проверки (как подтвердить выполнение)
    └── hotfix.md        ← шаблон хотфикса (как синхронизировать артефакты после релиза)
```

Каждый шаблон — Markdown-файл с подробными инструкциями и комментариями вида `<!-- ... -->`. Эти подсказки остаются только в шаблонах: в итоговых документах не должно быть служебных комментариев и незаполненных заглушек. В `verify.md` дополнительно описано, как проходить чек-лист задач, обновлять статусы в `tasks.md` и вести `verify-report.md`.

## Формат входных параметров

Шаблоны ssf получают параметры одной строкой в формате `@<feature>@ <контекст>`:

- значение между первыми двумя символами `@` используется как **FEATURE** и определяет папку фичи (`@payments@` → `payments`);
- всё, что следует после второго `@`, — это **CONTEXT**. Он может занимать несколько строк, включать дополнительные пояснения и ссылки на артефакты.

XML-подобные теги не используются: достаточно указать название фичи и контекст в описанном формате.

## Разница между Spec, Plan, Tasks, Verify и Hotfix в ssf

Путаница между этапами **spec**, **plan**, **tasks**, **verify** и последующим сопровождением часто случается, поэтому давай разберём чётко:

---

### 🟦 Spec (`spec.md`) = ЧТО и ЗАЧЕМ

- Это **бизнес-описание** и пользовательская ценность.
- Отвечает на вопросы:
  - **Зачем нужна эта фича?**
  - **Что пользователь получит?**
  - **Какие пользовательские сценарии и ограничения?**
- Язык — ближе к продукту/дизайну.

**Пример (Button):**

```md
**Цель:** Дать пользователю единый UI-элемент для действий (primary/secondary), доступный для всех платформ.

### User Stories

- Как пользователь, хочу нажать на кнопку, чтобы выполнить действие.
- Как дизайнер, хочу единый стиль кнопки, чтобы интерфейс выглядел консистентным.

### UX / Правила

- Состояния: normal, hover, disabled, loading.
```

👉 Спека должна быть понятна даже бизнес-аналитику или продакту.

---

### 🟩 Plan (`plan.md`) = КАК

- Это **технический план реализации**.
- Отвечает на вопросы:
  - **Какие данные нужны?**
  - **Какие API/контракты?**
  - **Какая архитектура и ограничения?**
  - **Какие риски?**
- Язык — технический, для разработчиков.

**Пример (Button):**

```md
### Источники данных / схемы

- UI-дизайн из Figma.
- Темы (light/dark) из theme provider.

### Контракты

- Props: variant, size, disabled, loading, onClick.
- onClick не срабатывает при disabled/loading.

### Архитектура / Компоненты

- Button.tsx — основной компонент.
- Button.styles.ts — стили через styled-components.
- Button.test.tsx — unit-тесты.

### Риски

- Возможны расхождения в темах.
```

👉 План должен быть понятен архитектору и ведущему разработчику.

---

### 🟨 Tasks (`tasks.md`) = КОНКРЕТНО ЧТО СДЕЛАТЬ

- Это **чек-лист шагов для команды**.
- Отвечает на вопрос:
  - **Что нужно закоммитить, в каком файле, какой тест написать?**
- Язык — максимально прикладной.

**Пример (Button):**

```md
- [ ] Создать Button.tsx с пропсами variant, size, disabled, loading.
- [ ] Добавить стилизацию через styled-components.
- [ ] Добавить состояния hover, focus, loading.
- [ ] Написать unit-тесты (react-testing-library).
- [ ] Добавить сторис в Storybook.
```

👉 Tasks должны быть понятны джуну: бери и делай.

---

### 🟥 Verify (`verify.md`) = ПРОВЕРЯЕМ И ПОДТВЕРЖДАЕМ

- Это **отчёт об автоматической проверке задач** и решение по архивированию фичи.
- Отвечает на вопросы:
  - **Что было проверено и с каким результатом?**
  - **Какие несоответствия найдены и что с ними делать?**
  - **Можно ли переносить фичу в архив ssf?**
- Язык — фактологичный, с ссылками на артефакты.

**Пример (Button):**

```md
## Результаты проверки задач

- ✅ Создать Button.tsx с пропсами variant, size, disabled, loading. (см. PR #123)
- ❌ Написать unit-тесты (react-testing-library). Нет теста на состояние loading.

## Журнал несоответствий

- 2024-05-12: Отсутствует тест на loading, требуется доработка.

## Решение по архивированию

- Повторная проверка после добавления тестов.
```

👉 Verify опирается на `tasks.md`: обновляет чекбоксы, фиксирует логи и рекомендует следующие шаги.

---

### 🟧 Hotfix (`hotfix.md`) = АКТУАЛИЗИРУЕМ ПОСЛЕ РЕЛИЗА

- Это **автоматизированное обновление артефактов** после хотфикса или пострелизной доработки.
- Отвечает на вопросы:
  - **Какой инцидент или изменение обрабатываем и зачем?**
  - **Что нужно скорректировать в спецификации, плане, задачах и отчёте проверки?**
  - **Какие проверки выполнены и что осталось сделать после хотфикса?**
- Язык — прагматичный: фиксируем факты, статусы задач и ссылки на подтверждения.

**Пример (Hotfix):**

```
Используй шаблон из `spec/core/hotfix.md`.
@team-collaboration@ Хотфикс синхронизации комментариев: устранить потерю сообщений в реальном времени
Контекст: добавить запись о хотфиксе, обновить `tasks.md` и `verify-report.md`, зафиксировать дополнительные проверки по e2e.
```

👉 Hotfix держит `spec.md`, `plan.md`, `tasks.md` и `verify-report.md` в актуальном состоянии, автоматически проставляя чекбоксы и журналируя результаты хотфикса.

---

### 🧩 Разница в одном предложении

- **Spec** = формулируем _зачем_ и _что нужно для пользователя_.
- **Plan** = решаем _как технически реализовать_.
- **Tasks** = раскладываем _что конкретно закодить и проверить_.
- **Verify** = подтверждаем _что сделано_, фиксируем несоответствия и решение по архиву.
- **Hotfix** = синхронизируем артефакты после релиза, фиксируем статусы задач и проверки хотфикса.

## Примеры промптов

> В примерах подразумевается, что вы работаете в ассистенте, поддерживающем ssf и умеющем сохранять файл по инструкции `SAVE_AS`.

### Спецификация фичи

```
Используй шаблон из `spec/core/spec.md`.
@team-collaboration@ Разработать совместное редактирование досок для продуктовых команд
Доп. контекст: пользователи — менеджеры и дизайнеры, необходимо поддержать до 50 одновременных редакторов без конфликтов.
```

### План реализации

```
Используй шаблон из `spec/core/plan.md`.
@team-collaboration@ Сформировать технический план реализации совместного редактирования досок
```

### Список задач

```
Используй шаблон из `spec/core/tasks.md`.
@team-collaboration@ Подготовить дорожную карту задач для релиза совместного редактирования досок
```

### Проверка задач

```
Используй шаблон из `spec/core/verify.md`.
@team-collaboration@ Провести автоматическую проверку задач и решить, архивируем ли фичу
```

### Хотфикс после релиза

```
Используй шаблон из `spec/core/hotfix.md`.
@team-collaboration@ Хотфикс синхронизации комментариев — восстановить доставку сообщений в realtime каналах
```
